# public-facing api for app

# import * from './generated/prisma.graphql'

type Mutation {
	createItem(
		title: String
		desc: String
		image: String
		largeImg: String
		price: Int
	): Item!
}

type Query {
	items: [Item]!
}
